<template>
  <div>
    <!--
      THESIS: Face Value refuses the marketplace-grid default — a search
      isn't a listing, it's a specimen card in a personal collection.
      OWN-WORLD: cool light-gray ground (#eef0f2), near-black ink; pastel
      per-item cells (terracotta/sage/ochre/slate/plum) with a white
      keyline; Baloo 2 nameplates, JetBrains Mono glow readout for price.
      STORY: the visitor drops a photo, watches a tag pull taut and knot
      shut, then reads an honest price range once it resolves — never a
      sale, never a certified value.
      FIRST VIEWPORT: a dense grid of search cards under a letterhead
      add-a-find slot; the newest card leads, its glowing price chip the
      one warm note against all the cool gray.
      FORM: Collector's Roster (catalog world) — direction 3 of the
      grounded list, bolder register roll, seed 54dba2be. Shipped copy
      says "searches," never "roster," per user correction.
      FINISH: unreviewed and undocumented is unfinished; this build ends
      with the finish review, the verdict, DESIGN.md, and every shipping
      raster carrying its provenance.
    -->
    <NuxtRouteAnnouncer />
    <NuxtPage />
  </div>
</template>
